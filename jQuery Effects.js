//waits until page is loaded first
$(document).ready(function(){
    //change all the <a> tags to <button> elements. 
    $("a")
        .html( "<button> class="bottom_button">Button </button>" );
    });
    
});
    
    